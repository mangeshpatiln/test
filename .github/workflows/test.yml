
name: deployment
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: ssh connection checking
        uses: appleboy/ssh-action@master 
        with: 
          host: SSHINDE-LAPTOP1
          username: MangeshPatil
          key: ${{secrets.PRIVATE_KEY}}
          port: 22
          script: |
           java -version
