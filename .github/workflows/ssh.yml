name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@master
      with:
        host: mpatil@192.168.0.228
        username: mpatil
        password: mp@123DataSeer$
        port: 22
        script: whoami
