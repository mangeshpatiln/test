name: Command via SSH

on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
     - name: executing remote ssh commands using ssh key
       uses: appleboy/ssh-action@master
       with:
         host: test.detaseers.in
         username: mpatil
         key: ${{ secrets.PEM_KEY }}
         port: 22
         script: whoami
      
